# shared

Reusable code shared across the whole project.

## Folders

- `lib/`: non-visual TypeScript utilities and helpers.
- `server/`: server-only utilities for routes and docs-backed app data.
- `ui/`: shared visual primitives and low-level interface building blocks.

Anything placed here must stay generic and reusable across multiple layers.

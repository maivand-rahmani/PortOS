# settings

System preferences app for wallpaper and desktop-level shell controls.

## Files and folders

- `index.ts`: app metadata, window config, and optional status bar actions.
- `icon.tsx`: custom app icon for the Settings app.
- `theme.css`: app-scoped visual styles.
- `ui/`: settings interface for wallpaper and shell preferences.

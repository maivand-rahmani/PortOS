# files-preview

Side panel showing file metadata (size, dates, MIME type) and inline text content preview for supported file types.

## Files

- `files-preview.tsx`: preview panel with metadata display and lazy content loading.

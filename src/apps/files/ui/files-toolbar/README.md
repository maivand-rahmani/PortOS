# files-toolbar

Top toolbar with navigation (up), create (file/folder), visibility toggle, view mode switch, and search input.

## Files

- `files-toolbar.tsx`: toolbar component with action buttons and search field.

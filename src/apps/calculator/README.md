# calculator

Compact calculator app with real expression evaluation, a persistent working tape, and direct handoff into Notes or the AI agent.

## Folders and files

- `index.ts`: app metadata and lazy loader.
- `theme.css`: local calculator styling.
- `model/`: expression input rules, tape persistence, and calculator state orchestration.
- `ui/`: calculator workspace, tape, action bar, and keypad rendering.

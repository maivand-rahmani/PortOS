# system-overview

Internal demo app used to verify the step-3 OS runtime.

## Folders and files

- `index.ts`: app metadata and lazy loader used by the app registry.
- `model/`: structured content rendered by the app.
- `ui/`: application-specific TSX component.

This app stays intentionally small so the runtime can be tested without committing to a full application suite yet.

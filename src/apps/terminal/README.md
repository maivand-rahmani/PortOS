# terminal

Working terminal app with real command handling and app opening.

## Folders and files

- `index.ts`: app metadata and lazy loader.
- `theme.css`: local terminal theme.
- `ui/`: terminal emulator component.

# desktop-ai-teaser

Left-side desktop attraction widget for the AI agent.

## Files

- `desktop-ai-teaser.tsx`: visible shell-level CTA widget that introduces the AI agent, can be dragged around the desktop, and launches guided flows.

This component exists to make the AI agent the first obvious interaction on the desktop without auto-opening the app on boot, while still behaving like a movable desktop widget.

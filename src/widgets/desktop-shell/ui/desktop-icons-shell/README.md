# desktop-icons-shell

Desktop chrome surface group for the desktop shell.

## Files

- `desktop-icons-shell.tsx` — composes the DesktopAiTeaser attraction widget and DesktopIcons field into a single section, gated by `showDesktopChrome`.

This section is hidden during fullscreen workspace presentations and only renders when the desktop chrome is visible.

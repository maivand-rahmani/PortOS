# mac-menu-bar

Top system status bar for the desktop shell, styled as a compact macOS-like translucent menu bar.

## Files

- `mac-menu-bar.tsx`: renders active app actions, default desktop commands, runtime indicators, AI access, the `Space+K` system AI hint, app switcher access, notification center access, the live date/time, and the fullscreen overlay variant.

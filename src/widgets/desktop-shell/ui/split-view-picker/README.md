# split-view-picker

Chooser surface for selecting the second app in a split-view workspace.

## Files

- `split-view-picker.tsx`: renders the in-pane app chooser used while building a split view from a fullscreen space, with explicit pointer-event ownership inside the workspace overlay layer.

# desktop-manager

Desktop icon state manager and spatial grid engine.

## Files

- `index.ts`: barrel exports for the desktop manager module.
- `desktop-manager.types.ts`: desktop icon positions, sort configuration, view mode, rename state, and full manager state types.
- `desktop-manager.grid.ts`: pure-function spatial engine for grid snapping, layout computation, occupancy tracking, collision resolution, and cleanup sorting.

# processes

System runtime layer for Portfolio OS.

## Folders

- `os/`: the core browser OS runtime, managers, and orchestration contracts.

Put global system behavior here. Widgets and apps can consume this layer, but they should not replace it.
